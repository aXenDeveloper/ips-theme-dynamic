<ips:template parameters="$table, $title=''" />
<div class='ipsBox' data-controller='core.front.modcp.reportList'>
	{{if $title}}
		<h2 class='ipsType_sectionTitle ipsType_reset ipsClear'>{lang="$title"}</h2>
	{{endif}}
	{$table|raw}
</div>